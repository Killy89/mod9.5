var itemButton = document.getElementsByClassName('button');

for (var i = 0; i < itemButton.length; i++) {
    var nameButton = itemButton[i].innerText;
    var text = nameButton;
    console.log(text);
}
